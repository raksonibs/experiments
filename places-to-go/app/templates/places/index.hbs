<h1> Places <div class="form-horizontal form-group form-group-lg row">
  <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
  {{input type="email" value=placeName class="form-control" placeholder="Please type your place to visit." autofocus="autofocus"}}
  </div>
  <div class="col-xs-10 col-xs-offset-1 col-sm-offset-0 col-sm-4 col-md-3">
    <button class="btn btn-primary btn-lg btn-block" disabled={{isDisabled}} {{action 'savePlace'}}>Create Place</button>
  </div>

</div> Go </h1>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>
        Name
        <br><small class="small not-bold">(Click on name for editing)</small>
      </th>
      <th class="vtop">Actions</th>
    </tr>
  </thead>
  <tbody>
    {{#each model as |place|}}
    <tr>
       {{#if place.isEditing}}
         <td>
          <form {{action "savePlace" place on='submit'}} class="form-inline">
            {{input value=place.name}}
            <button type="submit">
              Save
            </button>
          </form>
         </td>
       {{else}}
        <td>
          {{place.name}}          
        </td>
       {{/if}}

       <td>
         <button class="edit" {{action 'editPlace' place}}>Edit This Place!</button>
         <button class="destroy" {{action 'destroyPlace' place}}>Delete Place!</button>
       </td>

    </tr>
    {{/each}}
  </tbody>
</table>

